# VoiceLock.py — Local Voice Authentication for Firefly
import numpy as np
from cryptography.fernet import Fernet
import torch
from torchaudio import load
# ... (full code from repo — too long for here, but paste the whole thing)
