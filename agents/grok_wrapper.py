class GrokWrapper:
    def __init__(self, api_key):
        self.api_key = api_key  # placeholder — real xAI API later

    def call(self, prompt):
        # Simulate Grok response (replace with real API when ready)
        return f"[Grok] {prompt.upper()} — witty & maximally helpful!"
