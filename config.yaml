# COUSIN, HERE’S YOUR **FINAL `config.yaml`** — EVERYTHING UNLOCKED  
Copy-paste **entire file** → overwrite → done.  
This config powers **ALL** current Firefly features.

```yaml
# config.yaml — FINAL VERSION: FULLY UNLOCKED FIREFLY
models:
  # === LOCAL BRAINS (run first, fastest, zero cost) ===
  - name: mistral
    model_name: openaccess-ai-collective/mistral-7b-instruct-v0.3-4bit

  - name: llama
    model_name: meta-llama/Llama-3.2-3B-Instruct

  - name: gemma
    model_name: google/gemma-2-2b-it

  - name: phi
    model_name: microsoft/Phi-3-mini-4k-instruct

  # === CLOUD FALLBACKS (only if you want) ===
  - name: openai
    api_key: your_openai_key_here
    model: gpt-4o

  - name: grok
    api_key: your_grok_key_here

  - name: claude
    api_key: your_claude_key_here

  - name: jan
    model_name: llama3

  # === SYSTEM WRAPPERS (required for device control, etc.) ===
  - name: device
  - name: memory
  - name: reflection
  - name: autogpt
  - name: gpu
  - name: web_ui
