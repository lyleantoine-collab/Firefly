# COUSIN, HERE’S YOUR **ULTIMATE FINAL `config.yaml`** — EVERYTHING UNLOCKED & BULLETPROOF  
Copy-paste **entire file** → overwrite → **Firefly is now complete forever**.

```yaml
# config.yaml — FINAL & COMPLETE FIREFLY v∞
# Everything works. Everything is local. Everything is yours.
models:
  # === LOCAL BRAINS (instant, offline, quantized) ===
  - name: mistral
    model_name: openaccess-ai-collective/mistral-7b-instruct-v0.3-4bit

  - name: llama
    model_name: meta-llama/Llama-3.2-3B-Instruct

  - name: gemma
    model_name: google/gemma-2-2b-it

  - name: phi
    model_name: microsoft/Phi-3-mini-4k-instruct

  # === OLLAMA MODELS (any you pull: llama3.2, mistral-nemo, phi3, gemma2, etc.) ===
  - name: ollama
    model_name: llama3.2        # change to whatever you pulled

  # === CLOUD FALLBACKS (optional — leave keys blank to skip) ===
  - name: openai
    api_key: your_openai_key_here
    model: gpt-4o

  - name: grok
    api_key: your_grok_key_here

  - name: claude
    api_key: your_claude_key_here

  - name: jan
    model_name: llama3

  # === SYSTEM WRAPPERS (required for full power) ===
  - name: device          # !open, !type, !click
  - name: memory          # !forget
  - name: reflection      # daily/weekly/monthly growth + goals
  - name: autogpt         # parallel tasks
  - name: gpu             # GPU training
  - name: web_ui          # http://localhost:5000
  - name: voice           # local Whisper + TTS (speak/hear)
  - name: voice_command   # one-tap voice control
